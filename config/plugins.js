import gulpIf from 'gulp-if'
import gulpRename from 'gulp-rename'
import gulpSourcemaps from 'gulp-sourcemaps'

export const plugins = {
    if: gulpIf,
    rename: gulpRename,
    sourcemaps: gulpSourcemaps
}
